node_modules/
dist/
*.log
.DS_Store

# Local Netlify folder
.netlify

# Not a Deno project. This lockfile is written by tooling that probes for one,
# and committing it would imply a runtime nothing here uses.
deno.lock
